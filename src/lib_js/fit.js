
/* cssで補正しきれないレスポンシブ対応 */


//手動で調整すること

/*
function fit(){
	const width = document.body.offsetWidth;
	const height = document.documentElement.clientHeight;
	$ID("gaku-ura_base").style.width = "";
	$ID("gaku-ura_base").style.height = "";
	if (height < 600 && height < width){
		$ID("gaku-ura_base").style.width = parseInt(Math.min(height *2, width))+"px";
		$ID("gaku-ura_base").style.fontSize = "2vh"
	}
}
fit();
window.addEventListener("resize", fit);
*/

